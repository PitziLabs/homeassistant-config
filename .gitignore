# Home Assistant database — large binary, changes on every state update
home-assistant_v2.db
home-assistant_v2.db-wal
home-assistant_v2.db-shm

# Secrets — API keys, passwords, tokens
# Use secrets.yaml.example as documentation for required keys
secrets.yaml

# Internal HA state — auth tokens, device/entity registries, UI-managed config
.storage/

# Nabu Casa cloud credentials
.cloud/

# Python dependency cache — auto-managed by HA
deps/

# Runtime artifacts
.cache/
.ha_run.lock
*.log
*.log.*

# Text-to-speech cache
tts/

# OS and editor artifacts
.DS_Store
*.swp
*.swo
*~

# Stock HA blueprints — not customized, regenerated on install
blueprints/


# ESPHome build artifacts and development archives
esphome/.esphome/
esphome/archive/

# GitOps sync artifacts
.gitops-sync.lock

# HA context dump runtime artifacts
.context-mirror/
ha-context-dump.log
ha-context-dump.log.*

# Claude Code session state — ephemeral cowork worktrees
.claude/worktrees/

# GitHub PAT — credential, never commit
.gh-token

# Python bytecode
__pycache__/
*.pyc

# Kiosk frame grabs — throwaway snapshots from pve2's display
# (kiosk-snapshot, kiosk-preview, kiosk-serve, scrot dumps, comparisons)
/kiosk-*.png
/ha-login-*.png
/screen-time-render.png
